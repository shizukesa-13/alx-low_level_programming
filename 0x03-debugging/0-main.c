#include "main.h"
/* Function declaration */
void positive_or_negative(int i);
/**
 * main - only one main file to test with
 * Return: 0
 */
int main(void)
{
int i;
i = 0;
positive_or_negative(i);
return (0);
}

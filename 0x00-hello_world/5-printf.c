#include <stdio.h>
/**
 * main - A c program that prints a line using the funtion printf
 * Return: 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
